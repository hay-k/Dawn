{{!< default}}

<main class="site-main">

    <div class="post-feed gh-feed gh-canvas">
        {{#foreach posts}}
            {{> "loop_homepage"}}
        {{/foreach}}
    </div>

    {{pagination}}

</main>